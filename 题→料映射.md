# 题 → 料 映射

刷题卡住时，按这张表去读对应 agent 的源码。路径都在 [`资料/agent-frameworks/`](资料/agent-frameworks/)（项目内目录，被 `.gitignore` 忽略；缺了跑 `bash 资料/download.sh`）。

> `claude-code` 出现最多——它一个仓库几乎覆盖所有桶，是工程化标杆，建议作为主线精读。源码导读见 [`资料/源码导读.md`](资料/源码导读.md)。

| 题库分类 | 首选源码（agent → 具体位置） | 为什么 / 看什么 |
|---|---|---|
| 🏗️ **架构 / 规划 / 多 Agent** (1010) | claude-code → `src/QueryEngine.ts`、`src/query.ts`（主循环）、`src/coordinator/`、`src/tasks/`（多/子 agent）；OpenHands → agent controller + runtime；AutoGPT → 自循环/规划；plandex → 长任务规划 | 一次请求怎么跑、ReAct 循环、子 agent/swarm 编排、计划-执行 |
| 📚 **RAG / 检索** (1107) | aider → `aider/`（repo map：代码库索引与上下文构建）；claude-code → `src/tools/`（Grep/Glob 检索工具）、`src/services/compact/`（上下文压缩） | 工程化"检索+喂上下文"的真实做法，不只向量库 |
| 🔧 **Tool / MCP / Skill** (664) | claude-code → `src/Tool.ts`、`src/tools.ts`、`src/tools/`、`src/services/mcp/`、`src/skills/`、`src/hooks/`（权限门禁）；cline / gemini-cli / goose → 各自 MCP 实现 | 工具抽象、注册、调用、MCP 协议、技能加载——claude-code 是标杆 |
| 🧠 **Memory（记忆与上下文）** (509) | claude-code → `src/memdir/`、`src/services/compact/`、`src/context.ts`；hermes-agent → 学习循环/经验记忆；AutoGPT → memory | 跨会话记忆、历史压缩、上下文拼装 |
| 📊 **Eval / 观测** (300) | OpenHands → `evaluation/` + `benchmark`（SWE-bench 等）；aider → `benchmark/`；claude-code → cost-tracker / telemetry | 真实 agent 怎么做评测基准与可观测 |
| 🚀 **AI 产品 / AI Coding** (272) | 全体 coding agents 对比：claude-code · codex · cline · crush · opencode · qwen-code · plandex · aider | 产品形态、交互/UX、provider 抽象、diff 应用的不同取舍 |
| ✍️ **Prompt / Context 工程** (225) | claude-code → `src/context.ts`（系统提示拼装）、`src/services/compact/`；aider → prompts / edit formats | 系统提示结构、上下文预算、编辑指令格式 |
| 🤖 **LLM 基础 / 训练 / 推理** (193) | hermes-agent → 训练/学习循环；各 agent → `services/api`（流式/推理调用，工程视角） | 源码侧主要是"调用与流式"；纯理论题（attention/LoRA/PPO）靠书本，源码作补充 |
| 🛡️ **Safety / 安全 / 幻觉** (84) | claude-code → `src/hooks/`（工具权限门禁）；OpenHands → sandbox 隔离；codex / cline → 审批 / 人类确认 | 权限模型、沙箱、HITL 是工程化安全的三板斧 |
| 📦 **未归类 / 杂项** (60) | 按题目内容临时归到上面对应桶 | —— |

## 说明

- 路径以模块名为准；`claude-code/src` 的结构稳定（与源码导读里的源码 MAP 一致）。其他 agent 给到顶层目录，进去再找。
- 这是 **v1 起点映射**，基于各 agent 的公认定位。随刷题深入，把"这道题在某 agent 里其实看 X 文件"的发现回填到这里，让映射越来越准。
